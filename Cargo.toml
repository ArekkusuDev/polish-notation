[package]
name = "polish-notation-core"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
name = "_core"

[dependencies]
pyo3 = { version = "0.29.0", features = ["abi3-py313"] }

[profile.release]
opt-level = 3
lto = true
strip = true
codegen-units = 1
panic = "abort"
